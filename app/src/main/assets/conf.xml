<?xml version="1.0" encoding="utf-8"?>
<!-- Ver1.0 李泽荣 2017/08/03 XC2910手持机配置文件 -->

<!-- 配置说明：
    conf 内部元素主要包含两个属性：mask 和 name 。
        mask ： 用于过滤可识别的标签。读出的标签信息第一个字母必须与 mask 的设置相匹配，才会对标签进行进一步解析。否则将提示标签无法识别。
        name ： APP所显示的标题。可自行定义。
    conf 内部元素的名称不能重复。
    conf 的 use 属性设置子元素名。用来确定 APP 具体使用了哪个子元素的配置。
 -->

<conf use="op1">
    <!-- 货车 -->
    <op1 mask="TQ!" name="XC2002-RHC型便携式标签读出器"/>

    <!-- 客车 -->
    <op2 mask="K" name="XC2002-RKC型便携式标签读出器"/>

    <!-- 机车 -->
    <op3 mask="JD" name="XC2002-RJC型便携式标签读出器"/>
</conf>
